# Multiple jQuery versions are installed via npm aliases for testing.
# Their respective jquery-migrate packages have conflicting peer
# dependency requirements on jquery, so strict resolution must be
# disabled.
legacy-peer-deps=true
save-exact=true
